import React from "react";
import styled from "styled-components";

/** 스펙부족으로 개발 미진행 */
const RadioMain = ({ children }) => {
  return <div>{children}</div>;
};

export const Radio = Object.assign(RadioMain, {});
